import React from 'react';
import {Button} from 'reactstrap';

export default class PlayList extends React.Component {
  render() {
    return (
      <Button color="info">Update List</Button>
    )
  }
}
